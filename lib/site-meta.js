/** Browser tab title on all pages. */
export const SITE_TITLE = "TailorApp";

/** Login card headline (logo + h1). */
export const LOGIN_HEADLINE = "Tailor App in BOC-E";

/** Login page logo (served from /public). */
export const SITE_LOGO_PATH = "/logo.webp";

/** Intrinsic size of logo.webp — keeps layout stable before paint. */
export const SITE_LOGO_WIDTH = 48;
export const SITE_LOGO_HEIGHT = 48;

/** Browser tab favicon (served from /public). */
export const SITE_FAVICON_PATH = "/favicon.webp";

export const SITE_LOGO_MIME = "image/webp";
export const SITE_FAVICON_MIME = "image/webp";

export const SITE_FAVICON_WIDTH = 32;
export const SITE_FAVICON_HEIGHT = 32;
